# What's this sample?

Sample created by using the standard Blazor template with
individual authentication. It has been modified to use RavenDb as the
identity store.

## Running the sample

To run the sample, follow these steps:
1. Execute `docker-compose up` to start the RavenDb container.
2. Run the application with `dotnet run`.